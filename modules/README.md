# Steep modules (generated)

This folder is intentionally **empty** in git. During the demo, Cursor writes Steep-as-code YAML files here (for example `transactions.yaml`, `customers.yaml`).

Ask Cursor: *"Using business-context.md, generate the Finance team's metrics into modules/."*

More context: [docs/README.md](../docs/README.md) (Steep Help links + guidance audit).
